<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.5.2">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2017-09-09T23:04:13+05:30</updated><id>/</id><title type="html">PHPBees</title><subtitle>PHPBees is programming blog created from a programmer and dedicated to a programmer.Tutorial focused on PHP Programming,JavaScript and it's framework.
</subtitle><author><name>Smit Shah</name></author><entry><title type="html">Socket Programming With Php</title><link href="/php/2010/241/socket-programming-with-php.html" rel="alternate" type="text/html" title="Socket Programming With Php" /><published>2010-08-29T12:38:00+05:30</published><updated>2010-08-29T12:38:00+05:30</updated><id>/php/2010/241/socket-programming-with-php</id><content type="html" xml:base="/php/2010/241/socket-programming-with-php.html">&lt;p&gt;To understand Socket Programming(SP) you have to understand Network Communication fundamental first and I assume that you already read it. :)&lt;/p&gt;

&lt;p&gt;Here’s a simple script for sending messages back and forth between a server and client.  At this point, the code is fairly rough because once it enters the while loop, it doesn’t stop but it can be modified and fixed.(TCP/IP) Example.&lt;/p&gt;

&lt;h5 id=&quot;the-server&quot;&gt;The Server&lt;/h5&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;error_reporting(E_ALL);
$address = &quot;127.0.0.1&quot;;
$port = &quot;10000&quot;;
$mysock = socket_create(AF_INET, SOCK_STREAM, SOL_TCP);
socket_bind($mysock, $address, $port);
socket_listen($mysock, 5);
$client = socket_accept($mysock);
echo &quot;Server started, accepting connections...\n&quot;;
$i = 0;
while (true == true)
{
$i++;
echo &quot;Sending $i to client.\n&quot;;
socket_write($client, $i, strlen($i));
$input = socket_read($client, 2048);
echo &quot;Response from client is: $input\n&quot;;
sleep(5);
}
echo &quot;Closing sockets...&quot;;
socket_close($client);
socket_close($mysock);&lt;/code&gt;&lt;/p&gt;

&lt;h5 id=&quot;the-client&quot;&gt;The Client&lt;/h5&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;error_reporting(E_ALL);
$address = &quot;127.0.0.1&quot;;
$port = 10000;
$socket = socket_create(AF_INET, SOCK_STREAM, SOL_TCP);
if ($socket === false) {
echo &quot;socket_create() failed: reason: &quot; . socket_strerror(socket_last_error()) . &quot;\n&quot;;
} else {
echo &quot;socket successfully created.\n&quot;;
}
echo &quot;Attempting to connect to '$address' on port '$port'...&quot;;
$result = socket_connect($socket, $address, $port);
if ($result === false) {
echo &quot;socket_connect() failed.\nReason: ($result) &quot; . socket_strerror(socket_last_error($socket)) . &quot;\n&quot;;
} else {
echo &quot;successfully connected to $address.\n&quot;;
}$i = 0;
while (true == true)
{
$i++;
echo &quot;Sending $i to server.\n&quot;;
socket_write($socket, $i, strlen($i));
$input = socket_read($socket, 2048);
echo &quot;Response from server is: $input\n&quot;;
sleep(5);
}
echo &quot;Closing socket...&quot;;
socket_close($socket);&lt;/code&gt;&lt;/p&gt;</content><author><name>Smit Shah</name></author><category term="Socket Programming" /><summary type="html">To understand Socket Programming(SP) you have to understand Network Communication fundamental first and I assume that you already read it. :)</summary></entry><entry><title type="html">Setting up multiple virtual hosts</title><link href="/miscellaneous/2010/241/setting-up-multiple-virtual-hosts.html" rel="alternate" type="text/html" title="Setting up multiple virtual hosts" /><published>2010-08-29T11:33:00+05:30</published><updated>2010-08-29T11:33:00+05:30</updated><id>/miscellaneous/2010/241/setting-up-multiple-virtual-hosts</id><content type="html" xml:base="/miscellaneous/2010/241/setting-up-multiple-virtual-hosts.html">&lt;p&gt;Few days ago I had face a problem,”How to use different domains on localhost?”so at that time i doesn’t know how to use different/multiple domain on localhost so i just put project on Development server and then i check the functionality.&lt;/p&gt;

&lt;p&gt;Today I got some time to find out “How to make multiple domain on localhost”
so i got solution as always :)&lt;/p&gt;

&lt;p&gt;The multiple hostnames such as projecta, projectb, projectc and projectc-beta2 can be set in either of these hosts configuration files (also referred to as hosts files) depending on your operating system:&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Windows: C:\WINDOWS\SYSTEM32\DRIVERS\ETC\HOSTS (assuming your Windows installation is in C:\WINDOWS). 
For Linux: /etc/hosts 
If you were to open these files in a text editor, you’ll see content similar to the following: 
127.0.0.1        localhost 
192.168.1.18     testhost.testdomain.com testhost&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;All hosts files will have the localhost entry which resolves to 127.0.0.1, also known as the loopback address. Basically, what the first line in the host file illustrated above tells us is to route all network packets addressed to localhost to the computer you’re using.
So to get our fake hostnames working, they each should have an entry resolving to 127.0.0.1 in your hosts file. Example as follows:&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;127.0.0.1        localhost
127.0.0.1        projecta
127.0.0.1        projectb
127.0.0.1        projectc
127.0.0.1        projectc-beta2
192.168.1.18     testhost.testdomain.com testhost&lt;/code&gt;&lt;/p&gt;

&lt;h5 id=&quot;test-the-configuration&quot;&gt;Test the Configuration&lt;/h5&gt;
&lt;p&gt;To test the configuration, you should ping the fake hosts and if you had followed the steps above correctly, you should get a response from 127.0.0.1 for the projecta, projectb, projectcand projectc-beta2 hosts. This concludes the host resolving section of this tutorial.
Configuring Apache&lt;/p&gt;

&lt;p&gt;The next step involves configuring Apache to handle requests to the hosts defined above. To do this, you’ll need to edit your Apache installation’s configuration file; httpd.conf. On a default Apache for Windows installation, this file will be in C:\Program Files\Apache Group\Apache\conf. For Linux, the location of the httpd.conf file usually depends on the distribution. On Slackware Linux, it’s in /etc/apache/.&lt;/p&gt;

&lt;p&gt;Open the httpd.conf file in your favourite text editor and look for a line that starts with DocumentRoot. This line sets the location for the files that will be served as your default web site.&lt;/p&gt;

&lt;p&gt;On Linux, it will look something like the following (Note that the actual location may differ on your setup):
DocumentRoot “/var/www/htdocs”
Whereas on Windows, it will be similar to (Note that the actual location may differ on your setup):
DocumentRoot “C:/Program Files/Apache Group/Apache2/htdocs”
Also note that for Windows, Apache can also use a normal slash (/) instead of the typical backslash () in the directory path. You should also enclose directory paths in quotes. From now on, we’ll refer to these directories as DocumentRoot.&lt;/p&gt;

&lt;p&gt;If the directories where you want to serve content for the hosts created earlier reside within the DocumentRoot directory, you merely need to define a VirtualHost declaration for each of them. Let’s assume that the files for http://projecta will be in the directory named projecta under the DocumentRoot directory (which will be /var/www/htdocs/projecta/for Linux or C:\Program Files\Apache Group\Apache2\htdocs\projecta\ for Windows).
Therefore, at the end of your httpd.conf file, append the following lines to create the projecta virtual host:&lt;/p&gt;

&lt;p&gt;For Linux:&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;DocumentRoot /var/www/htdocs/projecta
ServerName slackbox&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;For Windows:&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;DocumentRoot &quot;C:/Program Files/Apache Group/Apache/htdocs/projecta&quot;
ServerName slackbox&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;If the files are served from a directory outside of the DocumentRoot, you need to create a Directory declaration for each of the directories in addition to the VirtualHost declaration.&lt;/p&gt;

&lt;p&gt;For example, let’s assume that files from projectb will be served from /web/projectb (for Linux) or C:\web\projectb (for Windows). Therefore, the following lines should be appended to your httpd.conf file:&lt;/p&gt;

&lt;p&gt;For Linux:&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Options Indexes FollowSymLinks MultiViews
AllowOverride All
Order allow,deny
Allow from all
DocumentRoot /web/projectb
ServerName projectb&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;For Windows:&lt;/p&gt;

&lt;p&gt;&lt;code class=&quot;highlighter-rouge&quot;&gt;Options Indexes FollowSymLinks MultiViews
AllowOverride All
Order allow,deny
Allow from all
DocumentRoot &quot;C:\web\projectb&quot;
ServerName projectb&lt;/code&gt;&lt;/p&gt;

&lt;h5 id=&quot;testing-the-setup&quot;&gt;Testing the Setup&lt;/h5&gt;
&lt;p&gt;Once all of the above is done, you can now test your configuration. However, you need to restart the Apache service in order to apply the changes made above. Here’s how to do it if you’ve installed Apache as a service (9 times out of time, this is how it’s normally installed):&lt;/p&gt;

&lt;p&gt;For Linux (distros using BSD-style init like Slackware):
&lt;code class=&quot;highlighter-rouge&quot;&gt;/etc/rc.d/rc.httpd restart&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;For Linux (distros using sysv-style init like Red Hat):
&lt;code class=&quot;highlighter-rouge&quot;&gt;service httpd restart&lt;/code&gt;&lt;/p&gt;

&lt;p&gt;For Windows:&lt;/p&gt;

&lt;p&gt;Run: services.msc
Look for Apache in the services list and select it
Click the Restart Service button&lt;/p&gt;

&lt;p&gt;If you’ve followed through this guide closely, you should now have a working Apache installation with multiple named local sites.&lt;/p&gt;</content><author><name>Smit Shah</name></author><category term="apache" /><category term="server" /><summary type="html">Few days ago I had face a problem,”How to use different domains on localhost?”so at that time i doesn’t know how to use different/multiple domain on localhost so i just put project on Development server and then i check the functionality.</summary></entry></feed>